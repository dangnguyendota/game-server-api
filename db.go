package api

// Database xử lý các dữ liệu trong db, memcache, redis, ..vv
// Nó được import vào trong file run server.
// phải được ép kiểu sang struct ứng với import vào để sử dụng.
type Database interface {
}